import { Flex } from "@chakra-ui/react";
import LoginForm from "./LoginForm";

const Login = () => {
    return (
        <Flex justify={'center'}>
           <LoginForm />
        </Flex>
    );
};

export default Login;